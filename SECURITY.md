# Security policy

Do not commit passwords, API keys, tokens, private keys, or production data.

Report a suspected vulnerability privately through GitHub's **Security → Report a vulnerability** feature. Do not publish exploit details in a public issue.

The deployment pipeline scans the repository for exposed secrets, vulnerable dependencies, and configuration problems. High or critical findings block deployment.

